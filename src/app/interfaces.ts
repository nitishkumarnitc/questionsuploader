export interface Chapter {
  chapterId:number;
  chapterName:string;
}

export interface Topic{
  topicId:number;
  topicName:string;
}

export interface Subject{
  subjectId:number;
  subjectName:string;
}

export interface Branch{
  branchId:number;
  branchName:string;
}
